import { SplitlinesPipe } from './splitlines.pipe';

describe('SplitlinesPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitlinesPipe();
    expect(pipe).toBeTruthy();
  });
});
