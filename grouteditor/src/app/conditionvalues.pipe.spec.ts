import { ConditionvaluesPipe } from './conditionvalues.pipe';

describe('ConditionvaluesPipe', () => {
  it('create an instance', () => {
    const pipe = new ConditionvaluesPipe();
    expect(pipe).toBeTruthy();
  });
});
